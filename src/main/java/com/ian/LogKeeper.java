package com.ian;

public class LogKeeper {
    //responsible for keeping a log of every transaction the server receives.
    //depends on FileStocker to push bytes to /log
    //adds date/time to transaction
}
